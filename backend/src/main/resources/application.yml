spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/messaging}
app:
  jwt:
    secret: ${JWT_SECRET:development-only-secret-change-this-to-at-least-thirty-two-characters}
    expiration-seconds: 86400
